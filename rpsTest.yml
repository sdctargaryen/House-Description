config:
  target: 'http://18.222.186.85:3002'
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
scenarios:
  - flow:
    - loop:
        - get:
            url: "/api/desc/{{$randomNumber(1, 10000000)}}"