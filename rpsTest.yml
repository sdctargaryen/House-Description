config:
  target: 'http://localhost:3002'
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
scenarios:
  - flow:
    - loop:
        - get:
            url: "/api/pgdesc/{{$randomNumber(1, 10000000)}}"